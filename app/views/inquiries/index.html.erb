<h1><%= t('views.inquiries_title') %></h1>

<table>
  <thead>
    <tr>
      <th><%= t('activerecord.attributes.inquiry.accepted_datetime') %></th>
      <th><%= t('activerecord.attributes.inquiry.inquired_person') %></th>
      <th><%= t('activerecord.attributes.inquiry.contents') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @inquiries.each do |inquiry| %>
      <tr>
        <td><%= inquiry.accepted_datetime %></td>
        <td><%= inquiry.inquired_person %></td>
        <td><%= inquiry.contents %></td>
        <td><%= link_to 'Show', inquiry %></td>
        <td><%= link_to 'Edit', edit_inquiry_path(inquiry) %></td>
        <td><%= link_to 'Destroy', inquiry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to t('views.inquiry_create'), new_inquiry_path %>
<br>
<br>
